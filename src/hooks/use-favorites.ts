// Hook de favoritos (re-exportación para compatibilidad hacia atrás).
// El código nuevo debe importar useFavorites directamente desde el controlador.

// Re-export from controller for backward compatibility
export { useFavorites } from "@/controllers/use-favorites-controller";
